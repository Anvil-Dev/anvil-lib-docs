import{_ as s,o as t,c as i,ak as a}from"./chunks/framework.CE-Jf_2W.js";const k=JSON.parse('{"title":"Math Flat Syntax","description":"","frontmatter":{"title":"Math Flat Syntax","prev":false,"next":false},"headers":[],"relativePath":"en/posts/docs/math/flat-syntax.md","filePath":"en/posts/docs/math/flat-syntax.md","lastUpdated":1789582422000}'),n={name:"en/posts/docs/math/flat-syntax.md"};function d(o,e,r,l,c,h){return t(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="flat-expression-syntax" tabindex="-1">Flat Expression Syntax <a class="header-anchor" href="#flat-expression-syntax" aria-label="Permalink to “Flat Expression Syntax”">​</a></h1><p><code>FlatExpressionParser</code> parses text such as <code>x*2</code>, <code>2x</code> or <code>2$(cost)+1</code> into an expression tree, while <code>FlatExpressionWriter</code> (package-private) handles the opposite direction, writing it back. Parsing and writing back share the same set of rules for &quot;what kind of name can be written out&quot;, so the two sides cannot drift apart.</p><h2 id="literals-and-references" tabindex="-1">Literals and References <a class="header-anchor" href="#literals-and-references" aria-label="Permalink to “Literals and References”">​</a></h2><table tabindex="0"><thead><tr><th>Syntax</th><th>Meaning</th></tr></thead><tbody><tr><td><code>2</code>, <code>2.5</code>, <code>.5</code>, <code>1.</code></td><td>Numeric constant (both a leading and a trailing dot are accepted)</td></tr><tr><td><code>1e3</code>, <code>-5.0E-4</code></td><td>Exponent notation; the exponent must be 1–8 digits</td></tr><tr><td><code>x</code>, <code>y</code>, <code>z</code></td><td>The 0th, 1st and 2nd input value</td></tr><tr><td><code>x0</code>, <code>x1</code>, <code>x12</code></td><td>Input values referenced by index</td></tr><tr><td><code>$(name)</code></td><td>Input value referenced by name</td></tr><tr><td><code>$(name...)</code></td><td>The whole variadic list; only usable as an argument of a variadic function</td></tr><tr><td><code>sqrt(x)</code></td><td>Function call</td></tr><tr><td><code>anvillib:sqrt(x)</code></td><td>Function call with a namespace</td></tr></tbody></table><ul><li><code>$(name)</code> reads one number; when <code>name</code> is bound to a variadic it yields the <strong>maximum</strong> of the list</li><li><code>$(name...)</code> reads the <strong>whole list</strong> itself; it is not a number, so using it where a single number is required reports an error at evaluation time</li><li>A whole list passed to a variadic position is <strong>spread out</strong>: <code>min($(x...), 4)</code> with <code>x = [9, 2, 7]</code> is <code>min(9, 2, 7, 4)</code></li><li>The payload of <code>$(...)</code> is scanned up to the <strong>first</strong> <code>)</code>, with no handling of nesting or escaping, and whitespace on either side is stripped, so <code>$( cost )</code> is equivalent to <code>$(cost)</code></li><li><code>x</code> is an alias for <code>&lt;input 0&gt;</code>, <code>y</code> for <code>&lt;input 1&gt;</code> and <code>z</code> for <code>&lt;input 2&gt;</code>, and any other index is written <code>x&lt;number&gt;</code>; an index beyond the <code>int</code> range reports <code>input index is too large: &#39;&lt;number&gt;&#39;</code> at parse time</li><li><code>x</code> and <code>$(x)</code> are <strong>not the same thing</strong>: the former reads by index and the latter by name binding, and they only give the same result when the name and the position happen to coincide</li><li>The identifier character set starts with <code>[A-Za-z_]</code> and continues with <code>[A-Za-z0-9_:.]</code>, so <code>a.b</code> and <code>ns:name</code> are each <strong>one</strong> token, and <code>x.y</code> is taken as an unknown function rather than <code>x</code> times <code>y</code></li></ul><h2 id="operators" tabindex="-1">Operators <a class="header-anchor" href="#operators" aria-label="Permalink to “Operators”">​</a></h2><table tabindex="0"><thead><tr><th>Operator</th><th>Spelling</th><th>Precedence</th><th>Associativity</th></tr></thead><tbody><tr><td>Lambda arrow</td><td><code>-&gt;</code></td><td>lowest</td><td>right</td></tr><tr><td>Add, subtract</td><td><code>+</code> <code>-</code></td><td>1</td><td>left</td></tr><tr><td>Multiply, divide</td><td><code>*</code> <code>×</code> <code>·</code> / <code>/</code> <code>÷</code></td><td>2</td><td>left</td></tr><tr><td>Implicit multiply</td><td>juxtaposition</td><td>2</td><td>left</td></tr><tr><td>Unary plus/minus</td><td><code>+</code> <code>-</code></td><td>3</td><td>right</td></tr><tr><td>Power</td><td><code>^</code></td><td>4</td><td>right</td></tr></tbody></table><ul><li><strong>Implicit multiplication</strong>: juxtaposition multiplies, but only when the right-hand side starts with <code>(</code>, <code>$</code> or an identifier — <code>2x</code>, <code>2(x+1)</code>, <code>2$(cost)</code> and <code>x y</code> are all multiplications, whereas <code>2 3</code> is not (it reports <code>unexpected character</code> or <code>expected &#39;,&#39; or &#39;)&#39;</code>)</li><li><strong>Dot operators</strong>: the multiplication sign may be written <code>·</code> (U+00B7) or <code>×</code> (U+00D7), and the division sign <code>÷</code> (U+00F7); writing back always uses <code>*</code> and <code>/</code></li><li><strong>Unary plus/minus</strong> sits below <code>^</code> and above <code>*</code>: <code>-x^2</code> is <code>-(x^2)</code> and <code>-x*y</code> is <code>-(x*y)</code>; <code>-2^2</code> is <code>-(2^2)</code> (<code>-4</code>) as well, and a negative base has to be written <code>(-2)^2</code></li><li>A sign immediately followed by a number is absorbed into the literal (<code>-2</code>, <code>+1.5</code>, <code>-5.0E-4</code>), which is why <code>2^-2</code> is valid</li><li>Whitespace may be inserted freely and is skipped; even the <code>-</code> and <code>&gt;</code> of <code>-&gt;</code> may have whitespace between them (<code>a - &gt; b</code> is a lambda)</li></ul><p>Operators are hard-bound to built-in functions: <code>+</code>→<code>add</code>, <code>-</code>→<code>subtract</code>, <code>*</code>→<code>multiply</code>, <code>/</code>→<code>divide</code>, <code>^</code>→<code>pow</code>, and parsing an operator does <strong>not consult the registry</strong>.</p><h2 id="lambda" tabindex="-1">Lambda <a class="header-anchor" href="#lambda" aria-label="Permalink to “Lambda”">​</a></h2><p>A lambda is written <code>parameters -&gt; body</code> (a <code>-</code> immediately followed by <code>&gt;</code>), and is passed to another function as a value:</p><table tabindex="0"><thead><tr><th>Spelling</th><th>Meaning</th></tr></thead><tbody><tr><td><code>x -&gt; $(x)*2</code></td><td>Single-parameter lambda</td></tr><tr><td><code>(a, b) -&gt; $(a)+$(b)</code></td><td>Multi-parameter lambda</td></tr><tr><td><code>x... -&gt; min($(x...))</code></td><td>Variadic-parameter lambda</td></tr></tbody></table><ul><li>The parentheses may be omitted for a single parameter; the <strong>canonical form</strong> written back always drops them, so <code>(a) -&gt; $(a)</code> is written <code>a -&gt; $(a)</code></li><li>The text is only parsed as a lambda when the parameter list itself is valid, so subtraction is never mistaken for the arrow: <code>x-1</code> still parses as subtraction and is written back as <code>x-1</code></li><li>A parameter name ending in <code>...</code> is variadic, with the same rules as custom functions (at most one variadic, it need not be last, and its lower bound is 0); see <a href="./custom-function.html#variadic-parameters">Custom Functions</a></li><li>A parameter name must be an identifier; an empty parameter string such as <code>()</code> is reported as a &quot;missing parameter name&quot;, so a zero-parameter lambda can only be written in the object form</li><li>A lambda is a <strong>closure</strong>: its body can read both its own parameters and the names bound at the call site</li><li>A lambda binds the loosest, so its body needs no parentheses; a body that is itself a lambda parses right-associatively, and <code>x -&gt; y -&gt; $(x)+$(y)</code> is written back as the same text</li><li>A lambda is a value: it can be passed as an argument to another function and serializes as the function type <code>anvillib:lambda</code> in the object form; when it lands in an operator&#39;s operand position it gets a pair of parentheses added, because <code>-&gt;</code> binds looser than every operator and dropping them would read as another tree (<code>2(x -&gt; $(x))</code> is written back as <code>2*(x -&gt; $(x))</code>)</li></ul><h2 id="function-names-and-namespaces" tabindex="-1">Function Names and Namespaces <a class="header-anchor" href="#function-names-and-namespaces" aria-label="Permalink to “Function Names and Namespaces”">​</a></h2><p>A function name is always treated as a registry name first:</p><ol><li>A name without a namespace is completed to <code>anvillib</code>, and then: <ul><li>a hit against a built-in function name goes to the built-in function and has its argument count validated <strong>at parse time</strong>, for example <code>sqrt(x)</code></li><li>otherwise the <code>anvillib:function</code> datapack registry is queried, so the datapack function <code>triple</code> is simply written <code>triple(x)</code> in text</li></ul></li><li>A namespaced name queries the <code>anvillib:function</code> datapack registry first, and a function found there is used (a function in another namespace may therefore override a same-named built-in)</li><li>When the registry has nothing, the name falls back to a built-in of the same path, so <code>mymod:sqrt(x)</code> is equivalent to <code>sqrt(x)</code></li><li>When nothing matches, <code>unknown function &#39;&lt;name&gt;&#39;</code> is thrown</li></ol><p>Note that steps 1 and 2 have a <strong>different order</strong>: in the <code>anvillib</code> namespace the built-in function wins, so registering an <code>anvillib:sqrt</code> in a datapack will not be picked up by text either (which is exactly why the write-back side refuses to write such a function as a bare name). Names are lowercased before they are resolved, so <code>SQRT(x)</code> and <code>X5</code> are both readable, while an entry whose registry path contains uppercase letters cannot be referenced from text.</p><p>Variables take priority over functions: <code>x</code>, <code>y</code>, <code>z</code>, <code>x0</code>… are recognised as input value references first, so registering a function named <code>x</code> also makes it <strong>impossible</strong> to call it from text.</p><p>Writing back does the opposite: a function that can be written as a bare name always has its <code>anvillib</code> namespace omitted, while other namespaces are kept as they are. The test for &quot;can be written as a bare name&quot; is aligned exactly with the parser&#39;s rules (<code>FlatExpressionParser.isWritableFunctionName</code>, shared by the write-back and parse sides), and the following names cannot be written as bare names and can only fall back to the object form:</p><ul><li>The same name as a built-in function (<code>anvillib:sqrt</code>): the parser prefers the built-in</li><li>The same name as an input value (<code>x</code>, <code>y</code>, <code>z</code>, <code>x0</code>, <code>x12</code>): the parser <strong>recognises variables before functions</strong>, so <code>x0(5)</code> reads as <code>&lt;input 0&gt; * 5</code> and the function is never called at all</li><li>A path containing a character outside the identifier character set (<code>[A-Za-z_][A-Za-z0-9_:.]*</code>): <code>collision-free</code> (<code>-</code>) and <code>utils/triple</code> (<code>/</code>) both stop being read halfway through</li><li>A name ending in <code>.</code> (such as <code>x.</code>) or an empty name</li></ul><div class="language-java line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Parsing</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sqrt(x)&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">           // -&gt; anvillib:sqrt (a built-in function)</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mymod:triple(x)&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   // -&gt; mymod:triple (looked up in the datapack registry)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Writing back</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">anvillib</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sqrt       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// -&gt; &quot;sqrt&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mymod</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">triple        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// -&gt; &quot;mymod:triple&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br></div></div><h2 id="arity-validation" tabindex="-1">Arity Validation <a class="header-anchor" href="#arity-validation" aria-label="Permalink to “Arity Validation”">​</a></h2><p>The argument count of both built-in and datapack functions is validated <strong>at parse time</strong>, and the error message carries the position:</p><div class="language-java line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">FlatExpressionParser.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parseValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sqrt(x,1)&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, functions);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// IllegalArgumentException: function &#39;anvillib:sqrt&#39; Expected 1 arguments but got 2 at position 9 of expression &quot;sqrt(x,1)&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">FlatExpressionParser.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parseValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mymod:triple(1)&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, functions);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// IllegalArgumentException: function &#39;mymod:triple&#39; Expected 2 arguments but got 1 at position 15 of expression &quot;mymod:triple(1)&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>A datapack function cannot afford to wait until evaluation time: an expression with a mismatched count would parse cleanly and land in a saved file, until it throws deep inside a BE tick or a datapack load. The criterion is exactly the <code>parameters()</code> the function itself declares, the same declaration <code>IFunction.bind</code> uses at evaluation time, so reporting the error earlier does not change any spelling that already worked. The rule itself lives in <code>Parameters.checkArity(List&lt;IExpression&gt;)</code>, shared by parse time, <code>LibBuiltInFunctions.call</code> and <code>callChecked</code>.</p><p>A variadic parameter follows Java&#39;s varargs semantics: its <strong>lower bound is 0</strong>, it may be given no argument at all, and the function itself handles the case where it gets no value. So <code>min()</code> / <code>max()</code> are valid calls returning 0, and <code>&quot;x...&quot;: []</code> is an ordinary empty call as well.</p><div class="language-java line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">FlatExpressionParser.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parseValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;min()&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, functions).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">evaluate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Arguments.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 0.0</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">FlatExpressionParser.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parseValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;max()&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, functions).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">evaluate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Arguments.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 0.0</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>Fixed parameters are unaffected and not one of them may be missing; when fixed and variadic parameters are mixed, the lower bound is the number of fixed parameters:</p><div class="language-java line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// The declaration is (x..., last): last must be given a value, while x may be given none</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CustomFunction.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(List.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;x...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;last&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), NamedFunction.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">call</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;last&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parameters</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">minimumArity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 1</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CustomFunction.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(List.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;x...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), NamedFunction.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">call</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;x&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parameters</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">().</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">minimumArity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();              </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 0</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p><code>$(x...)</code> is a <strong>whole list</strong> and can only land in a variadic parameter slot, so it takes part in the validation by the following rule:</p><ul><li>The function <strong>has</strong> a variadic parameter: the list fits, and its length is only known at evaluation time, so parse time lets it through, while <code>IFunction.bind</code> decides against the real length whether it falls inside the range</li><li>The function has <strong>no</strong> variadic parameter: not one fixed parameter slot can take a list, so the call is invalid no matter how long the list is, and the &quot;count of non-spread arguments&quot; is reported directly</li></ul><p>Matching is counted by <strong>argument count</strong>: the variadic parameter first reserves the slots of the fixed parameters after it, and only what remains belongs to it, while <code>$(name...)</code> spreads out from the variadic slot and occupies a number of slots. So an empty list (<code>$(name...)</code> spreads out 0) takes up no argument slot and the fixed parameters after it still get their own value; conversely, when the number spread out exceeds what the variadic slot can take, it reports <code>provides N arguments but this position takes M</code>.</p><div class="language-java line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Parameters (x..., last), with x = []: the empty list takes no slot, last gets 1, and x is the empty list</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">FlatExpressionParser.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parseValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;f($(x...), 1)&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, functions);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Parameters (x..., last), with x = [5, 6]: the variadic has only one slot left, which cannot hold two</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// IllegalStateException: Spread[name=x] provides 2 arguments but this position takes 1</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">FlatExpressionParser.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parseValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;f($(x...), 1)&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, functions);</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><div class="language-java line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">FlatExpressionParser.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parseValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;min($(x...))&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, functions);        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// OK, min is a variadic function</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">FlatExpressionParser.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parseValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sqrt($(x...))&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, functions);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// IllegalArgumentException: function &#39;anvillib:sqrt&#39; Expected 1 arguments but got 0 at position 13 of expression &quot;sqrt($(x...))&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">FlatExpressionParser.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parseValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;mymod:triple($(x...))&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, functions);   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// triple is (a, b)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// IllegalArgumentException: function &#39;mymod:triple&#39; Expected 2 arguments but got 0 at position 21 of expression &quot;mymod:triple($(x...))&quot;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br></div></div><p>Building a call by hand follows the same rule, so it will not first build a call that only blows up at evaluation time:</p><div class="language-java line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">LibBuiltInFunctions.MIN.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">call</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(IExpression.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ref</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;x...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));                       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// OK</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">LibBuiltInFunctions.SQRT.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">call</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(IExpression.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ref</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;x...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));                      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// throws IllegalArgumentException</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">LibBuiltInFunctions.ADD.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">call</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(IExpression.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ref</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;x...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), ConstantFunction.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">call</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// add is (a, b): throws</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><p>When a name is <strong>not bound to a list</strong>, evaluation names it in the error, instead of giving a baffling &quot;argument count mismatch&quot;:</p><div class="language-java line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">FlatExpressionParser.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parseValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;min($(unbound...))&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, functions).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">evaluate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Arguments.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// IllegalArgumentException: $(unbound...) is not bound to a list</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>A name bound to an <strong>empty list</strong> is a valid empty call, distinguishable from a misspelled name:</p><div class="language-java line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">IExpression call </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> FlatExpressionParser.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parseValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;min($(empty...))&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, functions);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">call.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">evaluate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Arguments.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(List.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), List.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;empty&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), List.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Arguments.Value.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Many</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(List.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">of</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()))));</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 0.0</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><h2 id="canonical-write-back-form" tabindex="-1">Canonical Write-Back Form <a class="header-anchor" href="#canonical-write-back-form" aria-label="Permalink to “Canonical Write-Back Form”">​</a></h2><p>The write-back result is a <strong>canonical form</strong> and does not preserve the original text, but any write-back result re-parses into the same expression tree, and <strong>writing it once more still yields the same text</strong>. The writer actually tests this before returning: it reads its own text back and writes it again, falling back to the object form when the two disagree.</p><table tabindex="0"><thead><tr><th>Input</th><th>Write-back result</th></tr></thead><tbody><tr><td><code>2*x</code>, <code>2·x</code>, <code>x*2</code></td><td><code>2x</code>, <code>2x</code>, <code>x*2</code></td></tr><tr><td><code>2*3</code>, <code>2*0.5</code></td><td><code>2*3</code>, <code>2*0.5</code> (no juxtaposition between constants)</td></tr><tr><td><code>2^(3^4)</code></td><td><code>2^3^4</code></td></tr><tr><td><code>(2^3)^4</code></td><td><code>(2^3)^4</code></td></tr><tr><td><code>x0+x1+x2</code></td><td><code>x+y+z</code></td></tr><tr><td><code>add(x,1)</code></td><td><code>x+1</code></td></tr><tr><td><code>pow(x,2)</code></td><td><code>x^2</code></td></tr><tr><td><code>2*mymod:triple(x,1)</code></td><td><code>2mymod:triple(x,1)</code></td></tr><tr><td><code>2*e1(1)</code></td><td><code>2*e1(1)</code></td></tr><tr><td><code>2*$(a)</code></td><td><code>2*$(a)</code></td></tr><tr><td><code>-x</code></td><td><code>-x</code></td></tr><tr><td><code>0-(x*y)</code></td><td><code>-(x*y)</code></td></tr><tr><td><code>x*(2+1)</code></td><td><code>x*(2+1)</code></td></tr><tr><td><code>-0</code>, <code>-0.0</code></td><td><code>-0.0</code></td></tr><tr><td><code>0.0001</code></td><td><code>0.0001</code></td></tr><tr><td><code>10000000</code></td><td><code>10000000</code></td></tr><tr><td><code>-1.0E7</code></td><td><code>-10000000</code></td></tr><tr><td><code>-5.0E-4</code></td><td><code>-0.0005</code></td></tr><tr><td><code>(a) -&gt; $(a)</code></td><td><code>a -&gt; $(a)</code></td></tr><tr><td><code>2(x -&gt; $(x))</code></td><td><code>2*(x -&gt; $(x))</code></td></tr><tr><td><code>(x -&gt; $(x))*2</code></td><td><code>(x -&gt; $(x))*2</code></td></tr><tr><td><code>(x -&gt; $(x))^2</code></td><td><code>(x -&gt; $(x))^2</code></td></tr><tr><td><code>min($(x...))</code></td><td><code>min($(x...))</code></td></tr><tr><td><code>mymod:max(x,1)</code> (<code>mymod:max</code> is not registered)</td><td><code>max(x,1)</code> (falls back to the built-in function)</td></tr></tbody></table><p>Parentheses are added in the minimal number of places, by the rule of &quot;would they associate into a different tree at the same precedence&quot;. Negating a compound expression always adds a pair, written <code>-(x^y)</code> rather than <code>-x^y</code>, because otherwise the <code>-</code> would land on the whole power.</p><p>Juxtaposed multiplication (dropping the <code>*</code>) is only used when the right-hand side can follow directly, which means the right-hand side has to start with an identifier, a parenthesis or <code>$(name)</code>, and the left-hand side has to be an unsigned numeric constant. The decision does not stop at the first character but is <strong>measured</strong>: an <code>e</code>/<code>E</code> after a number is taken as an exponent, so <code>2*e1(1)</code> written as <code>2e1(1)</code> reads as <code>multiply(20, 1)</code>, changing the value silently without reporting an error, which is why such names fall back to the explicit multiplication sign.</p><p>The spelling of a number gives priority to &quot;reading back&quot;: an integer (with an absolute value below <code>9.223372036854776E18</code>) is written as an integer literal, <code>1e-4 ≤ |x| &lt; 1e7</code> is written in decimal with trailing redundant zeros removed, and the rest is left to <code>Double.toString</code> (so <code>1e-5</code> is written <code>1.0E-5</code>). Negative zero is written <code>-0.0</code> rather than <code>-0</code>: <code>-0</code> would be read as <code>0-0</code>, turning the value into positive zero.</p><h2 id="cases-that-cannot-be-written-back" tabindex="-1">Cases That Cannot Be Written Back <a class="header-anchor" href="#cases-that-cannot-be-written-back" aria-label="Permalink to “Cases That Cannot Be Written Back”">​</a></h2><p>When writing back fails, <code>FlatExpressionWriter.write</code> returns an empty <code>Optional</code> and <code>IExpression.CODEC</code> falls back to the object form accordingly (rather than writing text that cannot be read back), in the following cases:</p><ul><li>The tree contains an inlined function definition with no resolvable registry name (built-in functions, an inline <code>CustomFunction</code>, and directly constructed call nodes all belong to this category)</li><li>The tree contains a non-finite constant (<code>NaN</code>, infinity) — a literal that overflows to infinity (<code>1e99999</code>) is rejected at parse time as well, so that nothing can be read in that cannot be written back</li><li>A datapack registers a function with the same name as a built-in in the <code>anvillib</code> namespace (such as <code>anvillib:sqrt</code>): the parser considers that name to be the built-in, so writing the short name would silently substitute the built-in, and the object form is the only option</li><li>A zero-parameter lambda: an empty parameter string in text is read as a &quot;missing parameter name&quot;, while the object form&#39;s <code>parameters: []</code> is valid</li><li>A by-name read&#39;s payload cannot be written as <code>$(name)</code>: the name contains a character outside the identifier character set such as <code>)</code> or a space, ends with <code>.</code> (such as <code>x.</code>), or is empty. A <strong>named</strong> reference whose name itself ends in <code>...</code> (<code>Reference.Named(&quot;x...&quot;)</code>, <code>NamedFunction(&quot;x...&quot;)</code>) cannot be written either — the written <code>$(x...)</code> would be read as a Spread, changing the meaning from &quot;take one number&quot; to &quot;take a list&quot;</li><li>An index reference lands on a negative or extremely large index (such as <code>InputFunction(-1)</code>): the <code>x-1</code> that would be written cannot be read back</li></ul><p>Negating a negative constant (<code>0-(-0.5)</code>) <strong>can</strong> be written back as <code>-(-0.5)</code>: the parentheses guarantee that the invalid <code>--0.5</code> is never produced, and it reads back as the same tree. The write-back self-check also downgrades &quot;written out but not reading back as the same tree&quot; to the object form, so there is no need to worry about a name quietly changing meaning.</p><blockquote><p>Writing back is not a pure output operation: for the self-check it parses the text it wrote a second time (and therefore writes into the parse cache), at a cost of roughly twice the size of the tree.</p></blockquote><h2 id="parse-error-messages" tabindex="-1">Parse Error Messages <a class="header-anchor" href="#parse-error-messages" aria-label="Permalink to “Parse Error Messages”">​</a></h2><p>Calling <code>FlatExpressionParser.parseValue</code> directly throws <code>IllegalArgumentException</code>, whose message always carries the suffix <code>at position &lt;position&gt; of expression &quot;&lt;source text&gt;&quot;</code>:</p><table tabindex="0"><thead><tr><th>Situation</th><th>Message</th></tr></thead><tbody><tr><td>An unreadable character in the text</td><td><code>unexpected character &#39;&lt;character&gt;&#39;</code></td></tr><tr><td>The text ends early</td><td><code>unexpected end of expression</code></td></tr><tr><td><code>$</code> is not followed by <code>(</code></td><td><code>expected &#39;(&#39; after &#39;$&#39;</code></td></tr><tr><td><code>$(...)</code> contains no name</td><td><code>expected a name between &#39;$(&#39; and &#39;)&#39;</code></td></tr><tr><td><code>$(</code> is not closed</td><td><code>expected &#39;)&#39;</code></td></tr><tr><td>A parenthesis is not closed</td><td><code>expected &#39;)&#39;</code></td></tr><tr><td>No <code>(</code> after a function name (including an unknown bare identifier)</td><td><code>expected &#39;(&#39; after function &#39;&lt;name&gt;&#39;</code></td></tr><tr><td>A comma is missing between arguments</td><td><code>expected &#39;,&#39; or &#39;)&#39;</code></td></tr><tr><td>The function name is not found</td><td><code>unknown function &#39;&lt;name&gt;&#39;</code></td></tr><tr><td>A literal overflows to infinity</td><td><code>number out of range &#39;&lt;source text&gt;&#39;</code> (the position points at the end of the literal)</td></tr><tr><td>An invalid number spelling</td><td><code>invalid number &#39;&lt;source text&gt;&#39;</code></td></tr><tr><td>The index after <code>x</code> exceeds <code>int</code></td><td><code>input index is too large: &#39;&lt;number&gt;&#39;</code></td></tr><tr><td>An invalid lambda parameter name</td><td><code>invalid lambda parameter name &#39;&lt;name&gt;&#39;</code> / <code>expected a lambda parameter name</code></td></tr><tr><td>A mismatched argument count</td><td><code>function &#39;&lt;name&gt;&#39; Expected &lt;range&gt; arguments but got &lt;count&gt;</code></td></tr><tr><td>Too deep nesting</td><td><code>expression nests too deeply, the limit 512</code></td></tr></tbody></table><p>Going through <code>IExpression.CODEC</code> wraps these errors into <code>DataResult</code> errors: <code>Invalid expression: &lt;the message above&gt;</code>; when the input is not a string it is <code>Not a flat expression: &lt;input&gt;</code>; and when the <code>DynamicOps</code> is not a <code>RegistryOps</code> it is <code>Cannot access registry ResourceKey[minecraft:root / anvillib:function], use RegistryOps</code>.</p><h2 id="cache" tabindex="-1">Cache <a class="header-anchor" href="#cache" aria-label="Permalink to “Cache”">​</a></h2><p>Parse results are cached in groups keyed by function registry instance, so parsing the same text repeatedly against one registry does not re-parse it, and returns <strong>the same instance</strong> directly. Each group holds at most 512 entries, evicting least-recently-used entries beyond that, with no expiry time; for the part where the weak key cannot be relied on and for the cleanup timing, see <a href="./cache.html">Cache and Lifecycle</a>.</p><h2 id="nesting-depth-limit" tabindex="-1">Nesting Depth Limit <a class="header-anchor" href="#nesting-depth-limit" aria-label="Permalink to “Nesting Depth Limit”">​</a></h2><p>Parentheses, powers, function arguments and lambda bodies share one nesting counter, and going beyond 512 levels reports <code>expression nests too deeply, the limit 512</code>. Without that limit, tens of thousands of parentheses or <code>2^2^2^…</code> would make the recursive descent throw <code>StackOverflowError</code> — that is an <code>Error</code>, which <code>parseResult</code>&#39;s <code>catch (RuntimeException)</code> cannot catch, so it would pass straight through the codec into the datapack loading flow.</p><p>That 512 is a <strong>counter</strong> limit rather than a number of nesting levels: one level of parentheses or argument nesting passes through the three guards <code>parseLambda</code>, <code>parseUnary</code> and <code>parsePower</code> in turn and counts once at each, so <code>sqrt(sqrt(…))</code> can actually nest only <strong>169</strong> levels deep (level 170 reports the error); only a chain of unary signs counts once per level, allowing 512 signs. Moving any one guard silently changes the usable depth, and <code>FlatExpressionTest</code> pins both boundaries.</p><h2 id="api" tabindex="-1">API <a class="header-anchor" href="#api" aria-label="Permalink to “API”">​</a></h2><div class="language-java line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> final</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FlatExpressionParser</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Parses; the whole text may also be just a number or one by-name read</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> static</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> IExpression </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parseValue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(String </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">source</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, HolderGetter&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">IFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">functions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // The codec for flat text, which only accepts RegistryOps</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> static</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Codec&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">IExpression</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">codec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Clears the parse cache</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> clearCache</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Extracts the function registry from dynamic ops, returning null when unavailable</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> static</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> @Nullable HolderGetter&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">IFunction</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">functionGetter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(DynamicOps&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ops</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br><span class="line-number">12</span><br><span class="line-number">13</span><br></div></div><p><code>FlatExpressionWriter</code> is a package-private class, used only indirectly through the encoding path of <code>FlatExpressionParser.codec()</code>; its two externally visible members are <code>write(IExpression, HolderGetter&lt;IFunction&gt;)</code> (returning <code>Optional&lt;String&gt;</code>, where empty means the text cannot be written) and <code>input(int)</code> (index → <code>x</code>/<code>y</code>/<code>z</code>/<code>xN</code>).</p>`,63)])])}const g=s(n,[["render",d]]);export{k as __pageData,g as default};
